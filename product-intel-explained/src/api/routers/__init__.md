# Module Initialization (`src/api/routers/__init__.py`)

This file marks the directory as a Python package.

- **What it does**: Exposes the routers folder as a module, allowing clean imports.
- **Why it exists**: Enables packaging structure rules in Python.
- **Interview explanation**: "A simple `__init__.py` file to establish namespace boundaries for the API endpoints."
