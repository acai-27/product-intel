# Module Initialization (`src/api/schemas/__init__.py`)

This file marks the directory as a Python package.

- **What it does**: Exposes the schemas folder as a module, allowing clean imports.
- **Why it exists**: Enables packaging structure rules in Python.
- **Interview explanation**: "A simple `__init__.py` file to establish namespace boundaries for the API data transfer objects (DTOs)."
