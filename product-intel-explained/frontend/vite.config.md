# Vite Build Options (`frontend/vite.config.js`)

This file configures our frontend bundler and development server.

- **What it does**: Bundles, splits, and serves our React client application.
- **Why it exists**: Optimizes build performance and sets up routing rules for development.
- **Interview explanation**: "This file configures Vite to bundle our frontend assets, split code blocks, and set up development servers."
- **Concepts used**: Asset bundling, build optimization.
